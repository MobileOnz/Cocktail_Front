# Leave blank to preview the UI with a fixed, clearly labeled demo result.
API_BASE_URL=
# Optional: exact Authorization header value expected by your backend.
# Local development only; never use a personal shared token on a public deployment.
API_AUTHORIZATION=
PORT=5173
HOST=127.0.0.1
