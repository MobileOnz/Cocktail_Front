/**
 * 텍스트 표시 공용 유틸.
 *
 * 가이드 본문 등 일부 시드 콘텐츠에 마크다운 강조 마커(`**`, `__`)가 섞여 있는데
 * plain <Text> 로 렌더하는 화면에서는 마커가 그대로 노출된다("****칵테일은…").
 * 마크다운 렌더러를 쓰지 않는 화면에서는 이 함수로 마커를 벗겨서 보여준다.
 */
export const stripMarkdownEmphasis = (s?: string | null): string =>
  (s ?? '').replace(/\*{1,3}|__|~~/g, '');
